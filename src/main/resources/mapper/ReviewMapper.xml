<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.deushu.review.mapper.ReviewMapper">

    <select id="findReviews" resultType="com.deushu.review.dto.ReviewListResponse$ReviewItem">
        SELECT
            r.id,
            m.name AS memberName,
            r.title,
            r.content,
            r.rating,
            r.photo_url AS photoUrl,
            r.created_at AS createdAt
        FROM reviews r
        JOIN members m ON m.id = r.member_id
        WHERE r.store_id = #{storeId}
          AND r.deleted_at IS NULL
          <if test="cursor != null">
            AND r.id &lt; #{cursor}
          </if>
        ORDER BY r.id DESC
        LIMIT #{limit}
    </select>

</mapper>